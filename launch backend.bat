cd App
cd Backend
echo ---------------------------------------------------------------------
echo the server may look like its hanging, but it is not. 
echo ***WARNING***: restarting the server will clear all database tables.
echo ---------------------------------------------------------------------
call npm install
npm start